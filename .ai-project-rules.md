# ThingsBoard 项目规则与结构指南

> 本文档用于帮助 AI 助手快速理解项目结构，避免每次都需要扫描整个代码库。

**📌 路径说明**: 本文档中所有路径均为相对路径，相对于项目根目录。无论项目在哪个目录下，这些规则都适用。

## 项目概览

**项目名称**: ThingsBoard  
**版本**: 4.3.0-SNAPSHOT  
**类型**: IoT 平台 (后端 + 前端分离架构)  
**仓库路径**: `<项目根目录>` (本文档中所有路径均相对于项目根目录)

---

## 技术栈

### 后端
- **语言**: Java 17
- **框架**: Spring Boot 3.4.8
- **构建工具**: Maven
- **主要技术**: 
  - Redis (Jedis 5.1.5)
  - Cassandra (4.17.0)
  - JWT (0.12.5)
  - Guava, Commons Lang3

### 前端
- **语言**: TypeScript 5.5.4
- **框架**: Angular 18.2.13
- **状态管理**: NgRx (Store, Effects)
- **UI 库**: Angular Material 18.2.14
- **构建工具**: Angular CLI, esbuild
- **样式**: Tailwind CSS 3.4.15
- **主要库**:
  - Leaflet (地图)
  - ECharts (图表)
  - Ace Editor (代码编辑)
  - TinyMCE (富文本编辑)

---

## 目录结构规则

### 根目录结构

```
<项目根目录>/
├── application/          # Spring Boot 主应用
├── common/              # 通用模块
├── dao/                 # 数据访问层
├── rest-client/         # REST API 客户端
├── rule-engine/         # 规则引擎模块
├── transport/           # 传输层（MQTT, HTTP等）
├── netty-mqtt/          # MQTT 实现
├── msa/                 # 微服务架构相关
├── ui-ngx/              # Angular 前端应用 ⭐
├── docker/              # Docker 配置
├── monitoring/          # 监控配置
├── tools/               # 工具脚本
├── doc/                 # 文档
└── pom.xml              # Maven 父 POM
```

### 前端目录结构（ui-ngx/）

```
ui-ngx/
├── src/
│   ├── app/
│   │   ├── core/                    # 核心模块 ⭐
│   │   │   ├── api/                 # API 服务
│   │   │   ├── auth/                # 认证相关
│   │   │   ├── guards/              # 路由守卫
│   │   │   ├── http/                # HTTP 拦截器
│   │   │   ├── services/            # 核心服务（如 menu.service.ts）
│   │   │   ├── translate/           # 国际化
│   │   │   └── utils.ts             # 工具函数
│   │   ├── modules/                 # 功能模块 ⭐
│   │   │   ├── common/              # 通用组件
│   │   │   ├── dashboard/           # 仪表盘
│   │   │   ├── home/                # 主页
│   │   │   └── login/               # 登录
│   │   ├── shared/                  # 共享模块
│   │   ├── app.module.ts            # 根模块
│   │   └── app-routing.module.ts    # 路由配置
│   ├── assets/                      # 静态资源
│   │   ├── locale/                  # 国际化文件
│   │   ├── dashboard/               # 仪表盘模板
│   │   └── map/                     # 地图样式
│   └── tsconfig.app.json
├── angular.json                     # Angular 配置
├── package.json                     # NPM 依赖
├── tailwind.config.js              # Tailwind 配置
├── eslint.config.mjs               # ESLint 配置
└── tsconfig.json                   # TypeScript 配置
```

---

## 代码规范与约定

### Angular 代码约定

#### 1. 服务（Services）
- **位置**: `ui-ngx/src/app/core/services/`
- **命名**: `*.service.ts`
- **装饰器**: `@Injectable({ providedIn: 'root' })`
- **依赖注入**: 通过构造函数注入

**示例**:
```typescript
@Injectable({
  providedIn: 'root'
})
export class MenuService {
  constructor(
    private store: Store<AppState>,
    private router: Router
  ) {}
}
```

#### 2. 状态管理（NgRx）
- **Store**: 使用 `@ngrx/store`
- **选择器**: 通过 `select()` 使用 selector
- **模式**: ReplaySubject + Observable

**示例**:
```typescript
this.store.pipe(select(selectAuth), take(1)).subscribe(
  (authState: AuthState) => {
    // 处理状态
  }
);
```

#### 3. 路由监听
```typescript
this.router.events.pipe(
  filter(event => event instanceof NavigationEnd)
).subscribe(() => {
  // 处理路由变化
});
```

#### 4. 模块化
- **Core Module**: 单例服务、全局配置
- **Shared Module**: 可复用组件、指令、管道
- **Feature Modules**: 按功能划分（lazy loading）

### Java 代码约定

#### 1. 包结构
- `org.thingsboard.server.application` - 应用主类
- `org.thingsboard.server.dao` - 数据访问
- `org.thingsboard.server.common` - 通用工具
- `org.thingsboard.server.service` - 业务服务

#### 2. 配置
- Spring Boot 配置文件在 `application/src/main/resources/`
- 使用 `@Configuration` 和 `@Bean`

---

## 工作流规则

### 1. 前端开发
当处理前端任务时：
- **优先查找**: `ui-ngx/src/app/`
- **服务层**: `core/services/`
- **组件**: `modules/` 或 `shared/`
- **API 调用**: `core/api/`
- **状态管理**: `core/auth/` 或相关 state 文件

### 2. 后端开发
当处理后端任务时：
- **主应用**: `application/`
- **数据层**: `dao/`
- **业务逻辑**: 各模块的 `service` 包
- **REST API**: `rest-client/` 或 `application/src/main/java/.../rest/`

### 3. 国际化
- **前端**: `ui-ngx/src/assets/locale/locale.constant-*.json`
- **格式**: `zh_CN`, `en_US` 等
- **支持语言**: 中文、英文、法语、德语、日语等 20+ 种

### 4. 配置文件
- **Angular**: `angular.json`
- **TypeScript**: `tsconfig.json`
- **ESLint**: `eslint.config.mjs`
- **Maven**: `pom.xml`（多模块）

### 5. 样式
- **主要**: Tailwind CSS（实用优先）
- **Material**: Angular Material 主题
- **自定义**: SCSS 文件在组件同级目录

---

## 常见任务模式

### 🔍 查找菜单相关代码
```
路径: ui-ngx/src/app/core/services/menu.service.ts
模型: ui-ngx/src/app/core/services/menu.models.ts
```

### 🔍 查找认证相关代码
```
路径: ui-ngx/src/app/core/auth/
文件: auth.models.ts, auth.selectors.ts, auth.effects.ts
```

### 🔍 查找组件
```
位置: ui-ngx/src/app/modules/[feature]/
结构: [component-name].component.ts/html/scss
```

### 🔍 查找 API 服务
```
位置: ui-ngx/src/app/core/api/
命名: [entity].service.ts
```

### 🔍 查找后端 REST 端点
```
位置: application/src/main/java/org/thingsboard/server/controller/
命名: [Entity]Controller.java
```

---

## 依赖管理

### 前端依赖更新
```bash
cd /home/tang/study/thingsboard/ui-ngx
yarn install  # 安装依赖
yarn add [package]  # 添加依赖
```

### 后端依赖管理
```bash
cd /home/tang/study/thingsboard
mvn clean install  # 构建所有模块
mvn dependency:tree  # 查看依赖树
```

---

## 开发命令

### 前端
```bash
# 开发服务器
yarn start  # 在 http://0.0.0.0:4200

# 生产构建
yarn build:prod

# 代码检查
yarn lint

# 类型生成
yarn build:types
```

### 后端
```bash
# 编译
mvn clean package

# 运行测试
mvn test

# 跳过测试构建
mvn clean install -DskipTests
```

---

## 文件搜索优先级

### 优先级 1（高频修改）
- `ui-ngx/src/app/core/services/`
- `ui-ngx/src/app/modules/home/`
- `ui-ngx/src/app/modules/common/`
- `application/src/main/java/org/thingsboard/server/controller/`

### 优先级 2（中频修改）
- `ui-ngx/src/app/shared/`
- `ui-ngx/src/assets/locale/`
- `application/src/main/java/org/thingsboard/server/service/`

### 优先级 3（低频修改）
- `dao/`
- `common/`
- `ui-ngx/src/assets/dashboard/`

### 可忽略目录
- `node_modules/`
- `target/`
- `dist/`
- `.git/`
- `*.log`

---

## 代码示例模式

### Angular Service 模式
```typescript
import { Injectable } from '@angular/core';
import { Observable, Subject, ReplaySubject } from 'rxjs';
import { Store, select } from '@ngrx/store';

@Injectable({
  providedIn: 'root'
})
export class [Name]Service {
  private data$: Subject<T> = new ReplaySubject<T>(1);
  
  constructor(private store: Store<AppState>) {}
  
  public getData(): Observable<T> {
    return this.data$;
  }
}
```

### NgRx 模式
```typescript
// Selector
this.store.pipe(
  select(selectData),
  filter(data => !!data),
  take(1)
).subscribe(data => {
  // 处理数据
});
```

---

## 特殊注意事项

### 1. 许可证头
所有源文件必须包含 Apache License 2.0 头部（2016-2025）

### 2. 构建配置
- 前端使用 esbuild（Angular 18）
- 内存配置：`--max_old_space_size=8048`（开发）/ `4096`（生产）

### 3. 图标
- 使用 Material Design Icons (`@mdi/svg`)
- 图标元数据：`ui-ngx/src/assets/metadata/material-icons.json`

### 4. 地图
- 主要库：Leaflet + MapLibre GL
- 样式配置在 `ui-ngx/src/assets/map/`

### 5. 仪表盘
- 模板位于 `ui-ngx/src/assets/dashboard/`
- Widget 定义：`application/src/main/data/json/system/widget_types/`

---

## 快速参考命令

### 查找服务
```bash
grep -r "class.*Service" ui-ngx/src/app/core/services/
```

### 查找组件
```bash
find ui-ngx/src/app/modules -name "*.component.ts"
```

### 查找国际化 key
```bash
grep -r "translation.key" ui-ngx/src/assets/locale/
```

### 查找 API 端点
```bash
grep -r "@RequestMapping" application/src/main/java/
```

---

## 更新记录

- **创建日期**: 2025-10-22
- **版本**: 1.0
- **基于**: ThingsBoard 4.3.0-SNAPSHOT

---

## 使用建议

1. **首次使用**：阅读完整文档
2. **日常使用**：快速查阅"常见任务模式"和"文件搜索优先级"
3. **新功能**：参考"代码示例模式"
4. **疑问**：查看具体目录结构说明

**记住**：前端在 `ui-ngx/`，后端在 `application/` 和其他模块目录。

