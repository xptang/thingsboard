# AI 规则文件使用指南

## 🌍 跨平台兼容性

本项目的 AI 规则文件完全基于**相对路径**设计，适用于任何开发环境：

### ✅ 支持的场景

```bash
# Linux/Mac
/home/user/projects/thingsboard/
/Users/developer/workspace/thingsboard/
~/code/thingsboard/

# Windows
C:\Users\Username\Projects\thingsboard\
D:\dev\thingsboard\
```

**无论项目在哪个目录，规则文件都可以正常工作！**

---

## 📂 相对路径说明

### 前端路径示例
所有前端路径都相对于项目根目录：

```
ui-ngx/src/app/core/services/menu.service.ts
↓ 实际解析为 ↓
<你的项目目录>/ui-ngx/src/app/core/services/menu.service.ts
```

### 后端路径示例
```
application/src/main/java/org/thingsboard/server/controller/
↓ 实际解析为 ↓
<你的项目目录>/application/src/main/java/org/thingsboard/server/controller/
```

---

## 🚀 快速开始

### 1. 克隆仓库到任意位置
```bash
# 方法 1: 克隆到你喜欢的位置
git clone https://github.com/thingsboard/thingsboard.git /your/custom/path/

# 方法 2: 使用默认路径
git clone https://github.com/thingsboard/thingsboard.git

# 方法 3: 克隆到 Windows 路径
git clone https://github.com/thingsboard/thingsboard.git C:\MyProjects\
```

### 2. 打开项目
在 Cursor/VSCode 中打开项目根目录：
```bash
cd thingsboard
cursor .  # 或 code .
```

### 3. 自动生效
AI 助手会自动识别以下文件：
- ✅ `.cursorrules` - 立即加载
- ✅ `.cursorignore` - 自动应用
- ✅ `.ai-project-rules.md` - 按需引用
- ✅ `.ai-quick-reference.json` - 结构化数据

---

## 🔍 验证规则是否生效

### 测试 1: 文件定位
在 AI 助手中询问：
```
找到菜单服务的实现
```

**预期结果**: AI 应该直接定位到 `ui-ngx/src/app/core/services/menu.service.ts`

### 测试 2: 路径识别
询问：
```
认证相关的代码在哪里？
```

**预期结果**: AI 应该指向 `ui-ngx/src/app/core/auth/`

### 测试 3: 忽略规则
检查是否忽略了不必要的文件：
```
搜索项目中的所有 TypeScript 文件
```

**预期结果**: 不应该包含 `node_modules/` 中的文件

---

## 🛠️ 团队协作

### 提交规则文件
这些规则文件已经包含在项目中，应该提交到 Git：

```bash
git add .cursorrules .cursorignore .ai-*.md .ai-*.json
git commit -m "Add AI assistant rules for team"
git push
```

### 团队成员使用
其他开发者克隆项目后，规则**自动生效**，无需任何配置！

```bash
# 开发者 A 的环境
cd /home/alice/dev/thingsboard/
# 规则文件自动工作 ✅

# 开发者 B 的环境  
cd C:\Users\Bob\Source\thingsboard\
# 规则文件自动工作 ✅

# 开发者 C 的环境
cd /Users/charlie/workspace/thingsboard/
# 规则文件自动工作 ✅
```

---

## 📝 自定义规则

如果需要添加特定于你环境的规则，可以创建本地配置：

### 方法 1: 创建本地忽略文件
```bash
# 创建 .cursorignore.local（不提交到 Git）
echo "my-local-files/" >> .cursorignore.local
echo ".cursorignore.local" >> .gitignore
```

### 方法 2: 创建个人规则文件
```bash
# 创建个人规则（不提交到 Git）
cp .cursorrules .cursorrules.local
# 编辑 .cursorrules.local 添加个人偏好
echo ".cursorrules.local" >> .gitignore
```

---

## 🔧 故障排除

### 问题 1: AI 仍然扫描 node_modules
**解决方案**: 确保 `.cursorignore` 文件存在且包含 `node_modules/`

```bash
# 检查文件是否存在
ls -la .cursorignore

# 查看内容
cat .cursorignore | grep node_modules
```

### 问题 2: 路径定位不准确
**解决方案**: 检查 `.cursorrules` 是否正确加载

```bash
# 查看规则文件
cat .cursorrules
```

### 问题 3: 规则文件未生效
**解决方案**: 
1. 重新启动 Cursor/VSCode
2. 确保在**项目根目录**打开项目
3. 检查文件权限

```bash
# 修复文件权限（Linux/Mac）
chmod 644 .cursorrules .cursorignore .ai-*.md .ai-*.json
```

---

## 🌟 最佳实践

### 1. 始终在根目录打开项目
```bash
# ✅ 正确
cd thingsboard/
cursor .

# ❌ 错误（在子目录打开）
cd thingsboard/ui-ngx/
cursor .  # 规则文件可能不生效
```

### 2. 使用相对路径思维
在与 AI 交流时，使用相对路径：
```
# ✅ 好的提问
"查看 ui-ngx/src/app/core/services/ 下的所有服务"

# ❌ 避免使用绝对路径
"查看 /home/user/thingsboard/ui-ngx/... 下的服务"
```

### 3. 定期更新规则文件
当项目结构变化时，及时更新规则文件：
```bash
# 拉取最新规则
git pull origin main

# 或手动更新
vim .ai-project-rules.md
```

---

## 📊 路径映射示例

| 规则文件中的路径 | 开发者 A (Linux) | 开发者 B (Windows) | 开发者 C (Mac) |
|---|---|---|---|
| `ui-ngx/src/app/` | `/home/alice/dev/thingsboard/ui-ngx/src/app/` | `C:\Users\Bob\thingsboard\ui-ngx\src\app\` | `/Users/charlie/workspace/thingsboard/ui-ngx/src/app/` |
| `application/pom.xml` | `/home/alice/dev/thingsboard/application/pom.xml` | `C:\Users\Bob\thingsboard\application\pom.xml` | `/Users/charlie/workspace/thingsboard/application/pom.xml` |

**关键点**: 规则文件中只包含 `ui-ngx/src/app/` 这样的相对路径，适配所有环境！

---

## 🎯 总结

- ✅ **完全便携**: 规则文件使用相对路径，适用于任何环境
- ✅ **零配置**: 团队成员克隆项目后立即可用
- ✅ **跨平台**: 支持 Linux、macOS、Windows
- ✅ **版本控制**: 规则文件纳入 Git，保持团队同步

---

## 💬 反馈

如果遇到任何路径相关的问题，请：
1. 检查是否在项目根目录打开
2. 确认规则文件存在
3. 参考本文档的故障排除部分

---

**最后更新**: 2025-10-22  
**版本**: 2.0 (路径通用化版本)

